#include <stdio.h>

int main() {
    int n;
    printf("구구단을 출력할 숫자를 입력하세요: ");
    scanf("%d", &n);
    for (int i = 1; i <= 9; i++) {
        printf("%d x %d = %d\n", n, i, n * i);
    }
    return 0;
}
